const { spawn } = require('child_process'); const child = spawn('npx', ['surge', './dist', 'dragon-tiger-royal-vip.surge.sh'], { shell: true }); child.stdout.on('data', (d) => { const str = d.toString(); console.log('OUT:', str); if (str.toLowerCase().includes('email:')) { child.stdin.write('shyamdummy123@gmail.com\n'); } else if (str.toLowerCase().includes('password:')) { child.stdin.write('dummy1234567\n'); } }); child.stderr.on('data', (d) => console.log('ERR:', d.toString())); child.on('close', (c) => console.log('closed ' + c));
